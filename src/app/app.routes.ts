import { Routes } from '@angular/router';
import { TimelineComponent } from './pages/timeline/timeline';

export const routes: Routes = [
  { path: '', component: TimelineComponent }, // raíz mostrará el timeline
];
